// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "forge-std/Test.sol";

contract DummyTest is Test {
    function testDummy() public {
        // A test that does nothing, just exists to avoid errors
        assertTrue(true);
    }
}
